# 🏗️ Dream Team Architecture Overview

## System Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                         FRONTEND (React)                         │
│                                                                  │
│  ┌────────────────┐  ┌────────────────┐  ┌─────────────────┐  │
│  │  Landing Page  │  │  Auth Pages    │  │  Event Pages    │  │
│  │  - Join Event  │  │  - Login       │  │  - Create       │  │
│  │  - Create Event│  │  - Sign Up     │  │  - Dashboard    │  │
│  └────────────────┘  └────────────────┘  │  - Profile      │  │
│                                           │  - Matches      │  │
│  ┌────────────────┐  ┌────────────────┐  │  - Connections  │  │
│  │  Mobile Nav    │  │  Sidebar       │  └─────────────────┘  │
│  │  - Hamburger   │  │  - Desktop     │                        │
│  └────────────────┘  └────────────────┘                        │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
                                 │
                                 ▼
┌─────────────────────────────────────────────────────────────────┐
│                    SUPABASE CLIENT LIBRARY                       │
│                     (@supabase/supabase-js)                      │
│                                                                  │
│  ┌──────────────┐  ┌──────────────┐  ┌───────────────────┐    │
│  │ supabase.ts  │  │  auth.ts     │  │   events.ts       │    │
│  │ - Client     │  │  - signUp    │  │   - createEvent   │    │
│  │ - Types      │  │  - signIn    │  │   - joinEvent     │    │
│  └──────────────┘  │  - signOut   │  │   - getEvents     │    │
│                    └──────────────┘  └───────────────────┘    │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │              matches.ts                                   │  │
│  │  - generateMatchesForUser                                │  │
│  │  - getMatches                                            │  │
│  │  - expressInterest                                       │  │
│  │  - getMutualConnections                                  │  │
│  └──────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
                                 │
                                 ▼
┌─────────────────────────────────────────────────────────────────┐
│                      SUPABASE BACKEND                            │
│                  (PostgreSQL + Auth + Realtime)                  │
│                                                                  │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │                    AUTHENTICATION                         │  │
│  │  ✓ Email/Password                                        │  │
│  │  ✓ Session Management                                    │  │
│  │  ✓ JWT Tokens                                            │  │
│  │  ✓ OAuth (Google) - Optional                            │  │
│  └──────────────────────────────────────────────────────────┘  │
│                                                                  │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │                    DATABASE TABLES                        │  │
│  │                                                           │  │
│  │  ┌─────────────┐         ┌──────────────┐              │  │
│  │  │   users     │         │    events    │              │  │
│  │  │  - id       │         │  - id        │              │  │
│  │  │  - name     │         │  - name      │              │  │
│  │  │  - email    │         │  - code      │              │  │
│  │  └─────────────┘         │  - host_id   │              │  │
│  │         │                 └──────────────┘              │  │
│  │         │                        │                       │  │
│  │         ├────────────────────────┴───────────┐          │  │
│  │         │                                     │          │  │
│  │  ┌──────▼──────────┐              ┌──────────▼────────┐ │  │
│  │  │  participants   │              │ event_profiles    │ │  │
│  │  │  - user_id      │              │  - user_id        │ │  │
│  │  │  - event_id     │              │  - event_id       │ │  │
│  │  │  - joined_at    │              │  - role           │ │  │
│  │  └─────────────────┘              │  - skills_have    │ │  │
│  │                                    │  - skills_need    │ │  │
│  │                                    │  - bio            │ │  │
│  │                                    └───────────────────┘ │  │
│  │                                             │             │  │
│  │         ┌───────────────────────────────────┴───────┐   │  │
│  │         │                                           │   │  │
│  │  ┌──────▼────────┐                     ┌────────────▼──┐│  │
│  │  │   matches     │                     │ connections   ││  │
│  │  │  - user_a_id  │                     │  - user_a_id  ││  │
│  │  │  - user_b_id  │                     │  - user_b_id  ││  │
│  │  │  - reasons[]  │                     │  - status     ││  │
│  │  │  - bucket     │                     │  - event_id   ││  │
│  │  └───────────────┘                     └───────────────┘│  │
│  └──────────────────────────────────────────────────────────┘  │
│                                                                  │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │              ROW LEVEL SECURITY (RLS)                     │  │
│  │  ✓ Users can only see own matches                        │  │
│  │  ✓ Event data visible to participants                    │  │
│  │  ✓ Connections require authentication                    │  │
│  │  ✓ Hosts can manage own events                          │  │
│  └──────────────────────────────────────────────────────────┘  │
│                                                                  │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │                 REAL-TIME SUBSCRIPTIONS                   │  │
│  │  ✓ Live match updates                                    │  │
│  │  ✓ Connection notifications                              │  │
│  │  ✓ Event participant changes                             │  │
│  └──────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
```

## Data Flow Diagrams

### 1. User Registration & Authentication

```
┌──────────┐
│   User   │
└────┬─────┘
     │
     │ 1. Fill signup form
     ▼
┌────────────────────┐
│ SignUpPageSupabase │
└────────┬───────────┘
         │
         │ 2. Call signUp()
         ▼
┌───────────────────┐
│ supabase-auth.ts  │
└────────┬──────────┘
         │
         │ 3. Create auth user
         ▼
┌──────────────────┐
│ Supabase Auth    │
└────────┬─────────┘
         │
         │ 4. Create user profile
         ▼
┌──────────────────┐
│  users table     │
└────────┬─────────┘
         │
         │ 5. Return user + session
         ▼
┌──────────────────┐
│    useAuth()     │  ← Stores in React state
└────────┬─────────┘
         │
         │ 6. Navigate to dashboard
         ▼
┌──────────────────┐
│   Landing Page   │
└──────────────────┘
```

### 2. Create Event Flow (Host)

```
┌──────────┐
│   Host   │
└────┬─────┘
     │
     │ 1. Fill event form
     ▼
┌────────────────────┐
│ CreateEventPage    │
└────────┬───────────┘
         │
         │ 2. Call createEvent()
         ▼
┌───────────────────┐
│ supabase-events.ts│
└────────┬──────────┘
         │
         │ 3. Generate 6-char code
         │ 4. Insert into events table
         ▼
┌──────────────────┐
│  events table    │
└────────┬─────────┘
         │
         │ 5. Return event + code
         ▼
┌──────────────────┐
│ Event Dashboard  │  ← Show code to host
└────────┬─────────┘
         │
         │ Host shares code with participants
         ▼
┌──────────────────┐
│  Participants    │
└──────────────────┘
```

### 3. Join Event Flow (Participant)

```
┌──────────────┐
│ Participant  │
└──────┬───────┘
       │
       │ 1. Enter event code
       ▼
┌───────────────────┐
│   Landing Page    │
└────────┬──────────┘
         │
         │ 2. Call getEventByCode()
         ▼
┌──────────────────┐
│ supabase-events  │
└────────┬─────────┘
         │
         │ 3. Find event
         ▼
┌──────────────────┐
│  events table    │
└────────┬─────────┘
         │
         │ 4. Call joinEvent()
         ▼
┌──────────────────┐
│ participants     │  ← User added to event
└────────┬─────────┘
         │
         │ 5. Navigate to profile setup
         ▼
┌──────────────────────┐
│ EventProfileSetupPage│
└────────┬─────────────┘
         │
         │ 6. Fill profile (skills, role, bio)
         ▼
┌──────────────────┐
│ supabase-events  │
└────────┬─────────┘
         │
         │ 7. createOrUpdateEventProfile()
         ▼
┌──────────────────┐
│ event_profiles   │  ← Profile saved
└────────┬─────────┘
         │
         │ 8. Navigate to matches
         ▼
┌──────────────────┐
│   Match List     │
└──────────────────┘
```

### 4. Matching Algorithm Flow

```
┌──────────────────┐
│   Match List     │
└────────┬─────────┘
         │
         │ 1. Page loads
         ▼
┌──────────────────┐
│ supabase-matches │
└────────┬─────────┘
         │
         │ 2. generateMatchesForUser()
         ▼
┌──────────────────────────────┐
│  Get user's event_profile    │
└────────┬─────────────────────┘
         │
         │ 3. Get all other profiles in event
         ▼
┌──────────────────────────────┐
│  For each other profile:     │
│  - Compare skills_have ↔     │
│    skills_need               │
│  - Check role compatibility  │
│  - Calculate score           │
│  - Generate reasons          │
└────────┬─────────────────────┘
         │
         │ 4. Create match records
         ▼
┌──────────────────┐
│  matches table   │
│  - reasons[]     │
│  - bucket        │
└────────┬─────────┘
         │
         │ 5. Return matches
         ▼
┌──────────────────┐
│ Display cards    │
│ with "Why You    │
│ Might Click"     │
└──────────────────┘
```

### 5. Express Interest Flow

```
┌──────────────┐
│    User A    │
└──────┬───────┘
       │
       │ 1. Click "Explore Collaboration"
       ▼
┌──────────────────┐
│   MatchCard      │
└────────┬─────────┘
         │
         │ 2. Call expressInterest()
         ▼
┌──────────────────┐
│ supabase-matches │
└────────┬─────────┘
         │
         │ 3. Create connection
         ▼
┌──────────────────┐
│ connections      │
│ status: pending  │
└────────┬─────────┘
         │
         │ 4. Check for mutual interest
         ▼
┌──────────────────────────────┐
│ Does User B have connection  │
│ to User A?                   │
└────────┬─────────────────────┘
         │
         ├─── YES (Mutual!) ───┐
         │                     │
         │                     ▼
         │            ┌─────────────────┐
         │            │ Update both     │
         │            │ connections to  │
         │            │ status: accepted│
         │            └────────┬────────┘
         │                     │
         │                     │ 5. Notify both users
         │                     ▼
         │            ┌─────────────────┐
         │            │  🎉 It's a      │
         │            │  Match!         │
         │            └─────────────────┘
         │
         └─── NO (One-way) ───┐
                              │
                              ▼
                     ┌─────────────────┐
                     │ Keep connection │
                     │ as pending      │
                     │ (hidden from B) │
                     └─────────────────┘
```

### 6. View Connections Flow

```
┌──────────────┐
│    User      │
└──────┬───────┘
       │
       │ Navigate to Connections page
       ▼
┌──────────────────┐
│ ConnectionsPage  │
└────────┬─────────┘
         │
         │ Call getMutualConnections()
         ▼
┌──────────────────┐
│ supabase-matches │
└────────┬─────────┘
         │
         │ Query connections table
         ▼
┌─────────────────────────────┐
│ SELECT * FROM connections   │
│ WHERE status = 'accepted'   │
│ AND (user_a_id = X          │
│   OR user_b_id = X)         │
└────────┬────────────────────┘
         │
         │ Join with event_profiles
         ▼
┌──────────────────┐
│ Return mutual    │
│ connections with │
│ profile data     │
└────────┬─────────┘
         │
         │ Display connection cards
         ▼
┌──────────────────┐
│ Show name, role, │
│ skills, contact  │
│ info             │
└──────────────────┘
```

## Security Model

### Row Level Security (RLS) Policies

```
┌──────────────────────────────────────────────────���─────┐
│                      users table                        │
│  Policy: Anyone can view profiles (for matching)       │
│  Policy: Users can update only their own profile       │
└────────────────────────────────────────────────────────┘

┌────────────────────────────────────────────────────────┐
│                     events table                        │
│  Policy: Anyone can view events (for joining)          │
│  Policy: Only host can update/delete event             │
└────────────────────────────────────────────────────────┘

┌────────────────────────────────────────────────────────┐
│                  participants table                     │
│  Policy: View participants in events you've joined     │
│  Policy: Users can join/leave events                   │
└────────────────────────────────────────────────────────┘

┌────────────────────────────────────────────────────────┐
│                 event_profiles table                    │
│  Policy: View profiles in events you've joined         │
│  Policy: Users can manage only their own profiles      │
└────────────────────────────────────────────────────────┘

┌────────────────────────────────────────────────────────┐
│                    matches table                        │
│  Policy: Users can view ONLY matches for themselves    │
│  Policy: Users can delete their own matches            │
│  Note: No cross-user visibility!                       │
└────────────────────────────────────────────────────────┘

┌────────────────────────────────────────────────────────┐
│                  connections table                      │
│  Policy: Users can view connections they're part of    │
│  Policy: Users can create connections (express interest│
│  Policy: Both parties can update status                │
└────────────────────────────────────────────────────────┘
```

## Technology Stack

```
┌──────────────────────────────────────┐
│            FRONTEND                  │
│  • React + TypeScript                │
│  • Vite (build tool)                 │
│  • Tailwind CSS (styling)            │
│  • Motion (animations)               │
│  • Lucide React (icons)              │
└──────────────────────────────────────┘
                 │
                 ▼
┌──────────────────────────────────────┐
│         SUPABASE CLIENT              │
│  • @supabase/supabase-js             │
│  • Real-time subscriptions           │
│  • Automatic retries                 │
└──────────────────────────────────────┘
                 │
                 ▼
┌──────────────────────────────────────┐
│         SUPABASE BACKEND             │
│  • PostgreSQL 15                     │
│  • GoTrue (Auth)                     │
│  • PostgREST (API)                   │
│  • Realtime (WebSockets)             │
│  • Row Level Security                │
└──────────────────────────────────────┘
```

## Deployment Architecture

```
┌─────────────────────────────────────────────────┐
│              CDN (Vercel/Netlify)               │
│  • Serves static React build                   │
│  • Environment variables                        │
│  • SSL/TLS encryption                           │
└─────────────────┬───────────────────────────────┘
                  │
                  │ HTTPS
                  ▼
┌─────────────────────────────────────────────────┐
│            Supabase Cloud                       │
│  ┌──────────────────────────────────────────┐  │
│  │  PostgreSQL Database                     │  │
│  │  • Auto-backups                          │  │
│  │  • Connection pooling                    │  │
│  └──────────────────────────────────────────┘  │
│  ┌──────────────────────────────────────────┐  │
│  │  Authentication                           │  │
│  │  • JWT tokens                             │  │
│  │  • Session management                     │  │
│  └──────────────────────────────────────────┘  │
│  ┌──────────────────────────────────────────┐  │
│  │  REST API (PostgREST)                    │  │
│  │  • Auto-generated from schema            │  │
│  │  • Row-level security enforcement        │  │
│  └──────────────────────────────────────────┘  │
│  ┌──────────────────────────────────────────┐  │
│  │  Real-time Engine                        │  │
│  │  • WebSocket connections                 │  │
│  │  • Change data capture                   │  │
│  └──────────────────────────────────────────┘  │
└─────────────────────────────────────────────────┘
```

---

**Legend:**
- `┌─┐` = Component/System boundary
- `│` = Contains/Includes relationship
- `▼` = Data/Control flow
- `├──` = Branch/Alternative path
- `✓` = Feature/Capability

For implementation details, see:
- **SUPABASE_SETUP.md** - Database setup
- **INTEGRATION_GUIDE.md** - Code integration
- **README_SUPABASE.md** - API reference
